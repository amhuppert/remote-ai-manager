/**
 * The small mono trigger for disclosures inside the memory panels, matching
 * the section labels around them rather than the display-font recipe the
 * shared Collapsible trigger uses for page-level sections.
 */
export const MEMORY_DISCLOSURE_TRIGGER_CLASS =
  "group flex w-full cursor-pointer items-center gap-sm border-0 bg-transparent p-0 text-left font-mono text-[0.7rem] tracking-[0.05em] text-text-tertiary uppercase hover:text-text-primary focus-visible:[outline:2px_solid_var(--color-cyan)] focus-visible:outline-offset-2 max-768:min-h-[44px]";
