import type { Metadata } from "next";

export { default } from "@/features/_root/RootLayout";

export const metadata: Metadata = {
  title: "CC — Ground Control",
  description: "Command Center — Remote coding session control",
};
