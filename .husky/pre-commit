#! /bin/sh

set -e

# # Format and stage
# npx prettier --write . > /dev/null 2>&1
# git add -u
# 
# # Lint with auto-fix and stage
# if [ "$CLAUDECODE" = "1" ]; then
#   npx eslint . --fix --quiet --no-color --no-warn-ignored
# else
#   npx eslint . --fix
# fi
# git add -u
# 
# # Type check
# if [ "$CLAUDECODE" = "1" ]; then
#   tsc --noEmit --pretty false
# else
#   tsc --noEmit --pretty
# fi
# 
# # Run unit tests (vitest.config.ts handles AI reporter selection)
# npx vitest run --project unit
