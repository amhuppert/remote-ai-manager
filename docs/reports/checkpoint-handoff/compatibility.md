# Saved checkpoint compatibility verification

Verified against source HEAD `c538aa114f8910f37b922fe08fca647976170aa7` plus the test-only changes described below. Production code was unchanged. This report settles the saved-checkpoint task; it does not certify the combined live-provider evidence or final release gates.

## Coverage

- `src/lib/conversation-checkpoints/testing/pre-handoff-v1-payload.json` is a literal copy of the independently authored fixture in pre-feature commit `42946e4cc18de1b729653f679b042db6339a7279:src/lib/conversation-checkpoints/fork-repo.test.ts`. It retains outer schema/builder version 1, raw source boundary 42, historical Q-137 text and SHA-256 `41ff46f5b361ebb2eef8e97569877073d3330bcfdb12243b5369eec83b47efcc`. It is not generated by the current builder.
- `checkpoint-fork.integration.test.ts` adds twelve cases: pre-feature v1, included and seed-budget-omitted payloads × session/project × Claude/Codex destination. Real SQLite persistence, production detail=seed handlers, ordinary manager/pre-turn delivery and admitted fork delivery preserve exact seed bytes, hash, source coordinates and lineage. Destination capture is unavailable. The source operation, including frozen handoff provenance, remains identical after fork delivery and manager restart. Fork payload identity changes to the destination operation by the existing contract; all other payload fields stay identical.
- `checkpoint-handoff-history.integration.test.ts` adds four cases: included/seed-budget-omitted × session/project. Each observes the real freeze transaction while source continuity still exists, refreshes the rolling artifact, delivers the seed, performs a distinct later capture, restarts the manager with a fresh repository reader, then forks the first historical operation to both destinations and delivers its exact seed. Earlier seed and handoff provenance remain unchanged throughout. No capture is repeated by reads, restart or forks.
- Existing production-route tests cover baseline no-opt-in with zero capture submissions, scoped checkpoint reads, complete oversized tool exports, thinking opt-in, original inline/external image bytes and hashes, missing assets and wrong-scope refusals. Existing delivery tests cover queued one-time acceptance, exact seed insertion and ordinary memory redelivery. Existing builder tests cover source evidence references, advisory separation and whole-handoff budget omission.

## Registered checks

| Check | Run ID | Result |
| --- | --- | --- |
| History and checkpoint route handlers (2 files) | `vrun-fad0a056-97cd-44dc-9d53-3897df1fdb70` | passed |
| Capture HTTP/manager integration (1 file) | `vrun-161cc0e4-79da-45e1-9fde-fcea12cef5ff` | passed |
| Delivery integration and builder (2 files) | `vrun-a6b634a7-d7bb-40f0-898b-6ed9fb227d22` | passed |
| Saved-seed fork compatibility (1 file) | `vrun-d5dd3387-a871-4684-b135-d005f93f22de` | passed |
| Frozen handoff history (1 file, 4 cases) | `vrun-d20b395e-75b8-43dd-a4eb-ff5aad85b733` | passed |
| Registered changed format | `vrun-c75e27d4-a8c7-469f-8bdb-ca2ae1fe0e0d` | passed |
| Registered full typecheck | `vrun-9c956d28-602f-46b5-b3e3-4384c1c2b4d5` | passed |

All scoped tests used explicit file paths and `--require-match --json`. These are preservation tests over existing behavior; no production bug fix or fabricated red phase was needed. Initial history-test failures corrected test assumptions: Design 7 permits capture-transport closure before final retirement, and forks assign a new payload ID while preserving contents.

## Invariants and limitations

`seed-data-authority`: new history tests plant an agent approval claim and assert it is absent from recorded-evidence working state and recent dialogue; included content remains separately attributed with the no-approval/validation/completion caveat. Omitted content stays out of the seed. Inspection of the builder, source projection, pre-turn delivery and fork validator confirms no compatibility translation or capability requirement was added.

`evidence-legality`: these are deterministic production-manager/route tests with injected provider runtimes and isolated real SQLite. They are not authenticated live-provider evidence. No operator cleanup testimony, human approval or release verdict is inferred from them. The later combined-evidence task owns those judgments.

The pinned Requirements R11.3/R11.5 and Design 7 govern: private capture transport closure is distinct from source continuity retirement; the exact saved v1 seed is consumed without rebuilding. Post-check git status/diff showed only the intended tests, literal fixture and this report; no snapshot rewrites or production changes.
