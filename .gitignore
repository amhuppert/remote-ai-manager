# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/

# build
dist/
build/
src/lib/build-info/build-info.generated.ts

# env files
.env
.env.*
!.env.example

# debug
npm-debug.log*
.debug/

# misc
.DS_Store
*.tsbuildinfo

# local dev state (CC_CONFIG_DIR)
.config/

# CC's per-worktree artifact namespace (charter, shared docs, validation and
# dev-server logs, agent scratch) — ephemeral, never published
.cc/

# voice/prompt (local dev)
.voice-last.json
PROMPT.md

.cursor/ralph-scripts/
.ralph/**
!.ralph/AGENT.md
RALPH_TASK.md

.worktrees/

*storybook.log
storybook-static

xnotes/

memory-bank/
next-env.d.ts
.vscode/explorer-hide-presets.json
.vscode/settings.json
.playwright-mcp
.playwright-cli
ROADMAP.md

# screenshots / image artifacts at repo root (Playwright, ad-hoc captures).
# Legitimate images live under public/ or feature folders.
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif

# /design-sync (claude.ai/design) — transient build/verification state.
# The durable set (config.json, NOTES.md, conventions.md, previews/, overrides/,
# ds-entry.tsx, sb-config/, tsconfig.bundle.json) stays tracked.
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
.design-sync/full-styles.css
