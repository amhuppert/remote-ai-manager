node_modules/
.next/
out/
dist/
build/
bun.lock
package-lock.json
*.md
.design-sync/
.ds-sync/
ds-bundle/
claude-design/
.worktrees/
storybook-static/

# Generated by scripts/css-inventory.ts (the script is the source of truth; --check enforces freshness)
docs/reports/css-inventory.json

# Captured production history, not authored source: reformatting them would make
# a capture indistinguishable from an edit. See the directory's README.
src/lib/specs/legacy-import-fixtures/*.json

# Pre-D4 compatibility recordings, written by the harness (CC_UPDATE_COMPAT_RECORDINGS=1).
# Reformatting them would leave every regeneration dirty against the writer's output.
src/lib/workflow-graph/compat/recordings/
